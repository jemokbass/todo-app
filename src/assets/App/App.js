import React from 'react';

import Layout from '@src/components/Layout/Layout';
import Routes from './Routes';

const App = props => {
  return (
    <Layout>
      <Routes />
    </Layout>
  );
};

export default App;
